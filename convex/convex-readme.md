# Convex Functions

This directory contains Convex backend functions, schema, and auth setup for ShowTracker.

Run `npx convex dev` from the project root to configure/push functions and generate Convex typings.
